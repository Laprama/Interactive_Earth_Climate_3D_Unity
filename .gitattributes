*.png filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text
# Use Unix-style LF endings for all text files in repo
* text=auto

# Force Unity meta files to use LF
*.meta text eol=lf

# Force scripts to use LF
*.cs text eol=lf

